[package]
name = "benchmark"
version = "0.1.0"
authors = ["negamartin"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
midly = { path = ".." }
rimd = "0.0"
nom-midi = "0.5"
augmented-midi = "1"
